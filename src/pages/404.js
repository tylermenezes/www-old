import React from "react"
import Layout from "../templates/layout"

export default () => (
  <Layout title="Not Found">
    <h1>Not Found</h1>
    <p>Sorry, but that page isn't here.</p>
  </Layout>
)
